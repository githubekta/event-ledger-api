package com.example.eventgateway.config;

/**
 * Resilience4j is configured declaratively in application.yml via
 * resilience4j.circuitbreaker / retry / timelimiter instances named "accountService".
 * This class is a placeholder to keep the package layout aligned with the spec;
 * additional programmatic customisations can be added here in future.
 */
public class ResilienceConfig {
}

